
<%= subscribe_to "/messages/new/"+ @usernumber.to_s %>
<div id="Header">
</div>

<h1>Chat</h1>

<ul id="chat">
  <% unless @messages.blank? %>
 <%= render @messages %>
<%end%>
</ul>

<%= form_for Message.new, remote: true do |f| %>
  <%= f.text_field :content %>
  <%= f.submit "Send" %>
<% end %>
